# config/schedule.yml
aviator_stream_starter:
  # 🚨 ИЗМЕНЯЕМ: запускается только при старте Sidekiq/при необходимости, 
  # а не по расписанию
  cron: '0 0 1 1 *' # Например, запускаем раз в год (фактически отключаем)
  class: 'AviatorJob'
  enabled: true